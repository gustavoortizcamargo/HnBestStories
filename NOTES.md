# Notes

This project was generated in a sandbox that does not have the .NET SDK installed, so it was not compiled inside the sandbox.

Run locally with:

```bash
dotnet restore
dotnet build
dotnet test
```
